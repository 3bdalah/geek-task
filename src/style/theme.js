import { createTheme } from "@mui/material";
export const theme = createTheme({
  palette: {
    primary: {
      main: "black",
      accentColor: "#53ACFF",
      orange: "#D94111",
      green: "#118F4B",
    },
  },
});
